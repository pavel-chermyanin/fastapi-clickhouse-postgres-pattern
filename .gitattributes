# Force LF for shell scripts
*.sh text eol=lf
# Force LF for python scripts if needed (optional, but good for consistency)
# *.py text eol=lf
# Ensure images and other binary files are not treated as text
*.png binary
*.jpg binary
*.ico binary
