# FastAPI Universal Pattern 2026

Универсальный шаблон для создания масштабируемых FastAPI приложений с использованием современных практик и модульной архитектуры.

---

## 🚀 Быстрый старт (одним скриптом)

### 1. Развертывание (Первый запуск)
Эта команда подготовит всё окружение: создаст `.env` (если его нет), установит зависимости, сгенерирует схему архитектуры и запустит базы данных.
```bash
python scripts/deploy.py
```

### 2. Запуск проекта
Запускает API Backend и сервер визуализации System Map (с предварительной валидацией):
```bash
python run.py
```
*   **Swagger UI**: [http://localhost:8000/docs](http://localhost:8000/docs)
*   **Admin Panel**: [http://localhost:8000/admin](http://localhost:8000/admin)
*   **System Map**: [http://localhost:8080/db_schema.html](http://localhost:8080/db_schema.html)

## ⚙️ Управление окружениями (.env)

Проект поддерживает разделение настроек для разработки и продакшена. При первом развертывании (`python scripts/deploy.py`) создаются два файла:

1.  **.env.development**: Предзаполнен настройками для локальной разработки (локальный Postgres, ClickHouse). Работает "из коробки".
2.  **.env.production**: Копия дефолтных настроек. **Важно:** перед деплоем на сервер замените в нем значения на реальные (пароли, хосты БД).

### Запуск с выбором окружения
Вы можете явно указать, какое окружение использовать при запуске через `run.py`:

```bash
# Запуск в режиме разработки (по умолчанию используется .env.development)
python run.py --env development

# Запуск в режиме продакшена (используется .env.production)
python run.py --env production
```

*Если вы не указываете `--env`, приложение по умолчанию запустится в режиме `development`.*

---

## 🏗️ Архитектура

Проект построен на **Модульной (Feature-based) архитектуре**.

```text
src/
├── modules/
│   └── users/              # Весь функционал модуля "Пользователи"
├── core/                   # Общие настройки, базовые классы (BaseRepository)
├── db/                     # Подключения к БД (Postgres, ClickHouse)
└── api/v1/                 # Сборка всех роутеров в единый API
```

---

## 🐳 Работа с Docker (Контейнеры)

Вы можете запустить всё окружение (БД + FastAPI + Admin) одной командой:
```bash
docker-compose up -d --build
```
*Для остановки: `docker-compose down`*

---

## 🛠️ Команды по отдельности

Все команды ниже кроссплатформенны (используйте `python` из корня проекта):

### Запуск API (ручной)
```bash
python run.py
```

### Визуализация (System Map)
```bash
# Генерация данных (после изменения моделей)
python scripts/generate_schema.py

# Запуск сервера просмотра
python scripts/serve_schema.py
```

### Работа с зависимостями (Poetry)
```bash
# Добавление библиотеки
python -m poetry add <package_name>

# Синхронизация
python -m poetry install
```

### Базы данных (Миграции)
```bash
# Применить миграции
python -m alembic upgrade head

# Создать новую миграцию (авто)
python -m alembic revision --autogenerate -m "Add description"
```

---

## 🌐 Доступ к интерфейсам

| Сервис | URL | Описание |
| :--- | :--- | :--- |
| **API Backend** | [http://localhost:8000](http://localhost:8000) | Основной адрес API сервера |
| **Swagger UI** | [http://localhost:8000/docs](http://localhost:8000/docs) | Документация для фронтенда |
| **Admin Panel** | [http://localhost:8000/admin](http://localhost:8000/admin) | Управление данными через SQLAdmin |
| **System Map** | [http://localhost:8080/db_schema.html](http://localhost:8080/db_schema.html) | Карта архитектуры и схема БД |


---

## � Качество кода (Pre-commit)

В проекте настроены автоматические проверки качества кода перед каждым коммитом. Они описаны в файле `.pre-commit-config.yaml` и включают в себя:
- **Black & Isort**: Автоматическое форматирование и сортировка импортов.
- **Ruff**: Быстрый линтинг и исправление типичных ошибок Python.
- **Check-migrations**: Проверка наличия созданных миграций при изменении моделей.

### Автоматический запуск
При выполнении команды `git commit` хуки запускаются автоматически. Если проверка не пройдена, коммит будет отклонен до исправления ошибок.

### Ручной запуск
Вы можете запустить все проверки вручную для всех файлов проекта:
```bash
# Для Windows
.\venv\Scripts\python.exe -m pre_commit run --all-files

# Для Linux/macOS
./venv/bin/python -m pre_commit run --all-files
```

---

## �🛡️ Правила разработки

Все правила описаны в директории `.rules/`.
- **Комментарии и Docstrings**: Всегда пишутся на русском языке.
- **Типизация**: Обязательна (проверяется Mypy).
- **Миграции**: Блокируют коммит, если модели изменены, а миграция не создана.
